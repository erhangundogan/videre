/target
.DS_Store

# Editor / agent tooling state
.vscode/
.claude/

# Local-only MCP server config (shadcn component fetching)
.mcp.json

# All working documents are local-only: design specs, implementation plans,
# tech-debt notes, and docs/TECHNICAL.md (the deep-detail companion to README,
# kept out of the repo so the published docs stay approachable).
docs/

# Brainstorming skill's visual-companion mockups/state
.superpowers/

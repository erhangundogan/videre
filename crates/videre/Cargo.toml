[package]
name = "videre"
version = "0.24.1"
edition = "2021"
description = "Local-first media library CLI: duplicate detection, semantic search, face recognition, and EXIF date repair over a single SQLite database"
license = "Apache-2.0"
repository = "https://github.com/erhangundogan/videre"
homepage = "https://github.com/erhangundogan/videre"
readme = "../../README.md"
keywords = ["photos", "duplicates", "exif", "cli", "search"]
categories = ["command-line-utilities", "multimedia::images"]
# Test fixtures are ~2.2MB of sample media that nothing at build or run time
# needs; keeping them out of the published tarball keeps `cargo install` lean.
exclude = ["tests/fixtures/*"]

[lib]
name = "videre"
path = "src/lib.rs"

[[bin]]
name = "videre"
path = "src/main.rs"

[dependencies]
clap = { version = "4", features = ["derive"] }
blake3 = "1"
rayon = "1"
walkdir = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
chrono = { version = "0.4", features = ["serde"] }
image = { version = "0.25", default-features = false, features = [
    "jpeg",
    "png",
    "gif",
    "webp",
    "bmp",
    "tiff",
] }
kamadak-exif = "0.5"
roxmltree = "0.20"
filetime = "0.2"
rusqlite = { version = "0.40", features = ["bundled"] }
videre-core = { path = "../videre-core", version = "0.24.1" }
videre-ml = { path = "../videre-ml", version = "0.24.1" }
videre-api = { path = "../videre-api", version = "0.24.1" }
axum = "0.8"
tokio = { version = "1", features = ["full"] }
anyhow = "1"
rmcp = { version = "2.2", features = ["transport-io"] }
schemars = "1"
askama = "0.16"

[dev-dependencies]
tempfile = "3"
# Cross-process lock serialising tests that download shared model weights;
# see tests/common/mod.rs. Same version videre-core uses.
fs2 = "0.4"
rusqlite = { version = "0.40", features = ["bundled"] }
serde_json = "1"
videre-core = { path = "../videre-core", version = "0.24.1" }

[target.'cfg(target_os = "macos")'.dependencies]
candle-core = { version = "0.11", features = ["metal"] }

[target.'cfg(not(target_os = "macos"))'.dependencies]
candle-core = "0.11"
